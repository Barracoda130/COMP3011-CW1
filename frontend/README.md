# Frontend (planned Vue.js app)

This folder is reserved for a future Vue.js frontend.

Suggested future setup:
- Vite + Vue 3 + TypeScript
- API base URL pointing to `http://127.0.0.1:8000/api/v1`
- Auth token handling for protected endpoints
